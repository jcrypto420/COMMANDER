# Weather Oracle — capture result-note template

Use this immediately after the one-shot Pi capture verification run.

## Run metadata
- timestamp (UTC):
- timestamp (Central):
- capture directory:
- exit code:

## Required pass gate
- `nws_forecast` status:
- `nws_forecast` file:
- `openmeteo_models` status:
- `openmeteo_models` file:

## Useful manifest details
- `captured_at_utc`:
- `captured_at_central`:
- reused directory warning:
- missing source files:

## Short result
- pass/fail:
- one-line note:
- next action:
