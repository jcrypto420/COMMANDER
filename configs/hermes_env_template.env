# Hermes env TEMPLATE — copy to a local `.env` (which is git-ignored).
# Fill ONLY the providers you actually use. Placeholders only in this file.
# NEVER commit real keys. See SECURITY.md / PROVIDERS.md.

# --- Tier 1 cheap hosted (pick at least one for day one) ---
OPENROUTER_API_KEY=
NOUS_PORTAL_API_KEY=
VENICE_API_KEY=
HF_TOKEN=
GITHUB_MODELS_TOKEN=
TOGETHER_API_KEY=
GROQ_API_KEY=
DEEPINFRA_API_KEY=

# --- Tier 2 premium (only for hard/high-value tasks) ---
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# --- Tier 0 local (Ollama needs no key) ---
OLLAMA_BASE_URL=http://localhost:11434/v1

# --- Optional: GitHub MCP (Phase 2, see MCP_PLAN.md) ---
GITHUB_TOKEN=
